import React from 'react';
import SmokerIndex from '../../components/RecipeIndex';


const SmokerDirectory = props => {
    return(
        <h2>All of the Smokers</h2>
    );
};

export default SmokerDirectory;

